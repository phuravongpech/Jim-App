# frontend

## To test mock exercise fetching

Install json-server
```
npm install -g json
```

Start the server
```
json-server lib/data/db.json
```
(You must be inside the frontend directory)

Run flutter app
